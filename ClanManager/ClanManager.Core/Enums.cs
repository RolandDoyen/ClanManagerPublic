namespace ClanManager.Core
{
    public enum ClanRole
    {
        ClanLeader,
        CoLeader,
        Elder,
        Member,
        Recruit
    }

    public enum Role
    {
        SuperAdmin,
        Admin,
        User
    }
}
